# AutoGit 엔지니어링 감사 보고서

- 수신: Brian
- 일자: 2026-05-31
- 대상 경로: `/home/ubuntu-02/ai_project/AutoGit`
- 감사자: 자동화 코드 감사 (읽기 전용 + 저위험 검증 수정)

## 개요(범위 + 한계)

본 보고서는 AutoGit 단일 프로젝트에 대한 증거 기반 정적/부분 동적 점검 결과이다. 과장이나 추정을 사실로 기술하지 않았으며, 직접 실행·관찰한 항목은 "확인", 코드 판독에 근거한 추론은 "추정"으로 명시한다.

스택(확인):
- 데스크톱 GUI: `main.py` (PyQt6 기반, 약 42KB). 진입점 `python3 main.py` 또는 `run.sh`.
- REST API 서버: `api_server.py` (FastAPI + uvicorn, 포트 4018). 진입점 `python api_server.py`.
- 코어 패키지: `core/` (`git_manager.py`, `ollama_client.py`, `version_manager.py`, `config_manager.py`). `main.py`와 `api_server.py` 모두 `core.*`를 임포트(확인).
- 저장소 루트에 동명의 모듈 4개(`git_manager.py` 등)가 추가로 존재하나 어디서도 임포트되지 않음(확인). 사실상 미사용 중복본.
- 영속화: SQLite(`~/.gitautopush/commit_logs.db`), 설정 JSON(`~/.gitautopush/`).
- LLM: 로컬 Ollama REST API(`http://localhost:11434`), 모델 기본값 불일치(아래 참조).
- Git 연동: `git_manager.py`는 gitpython이 아닌 `subprocess` 기반 CLI 래퍼(확인).

한계:
- 공유 박스 제약상 GUI(`main.py`)는 **실행하지 않음**. 이 도구는 commit/push를 자동 수행하므로 임의 저장소를 변경·push할 위험이 있어 실행을 의도적으로 회피했다.
- Ollama 서버·모델이 없어 LLM 경로의 실응답은 검증 불가(서버 미가동 분기만 확인).
- `api_server.py`의 의존성(fastapi/uvicorn/pydantic)이 venv에 미설치되어 서버 기동은 수행하지 않음. 대신 동일 로직을 코어 모듈로 시뮬레이션해 검증.
- `data/`, `.git/`는 건드리지 않음. 모든 명령은 `timeout`으로 시간 제한.
- 브랜드 스크럽 흔적('WDLAB@2023-2026'/'WDLAB@2023-2026'/'WDLAB') 검색 결과 잔존물 없음(확인). 원복 작업 없음.

## 실행·테스트 결과

- Python: venv는 `venv/bin/python` = CPython 3.12.3 사용(확인). 인프라 버그(pyenv 3.12.11 asyncio.run SIGSEGV)에 해당하는 명령은 실행하지 않아 재현 안 됨.
- 의존성 점검(확인): venv 내 `PyQt6` OK, `requests` OK. `git`(gitpython) FAIL, `fastapi`/`pydantic`/`uvicorn` 모두 FAIL(미설치).
- 바이트 컴파일(확인): `main.py`, `api_server.py`, `core/*.py` 전체 `py_compile` 통과.
- 코어 임포트(확인): `core.git_manager / ollama_client / version_manager / config_manager` 정상 임포트.
- 린트/타입체크: 프로젝트에 ruff/flake8/mypy 설정 및 도구 없음(확인). 별도 설치는 환경 제약상 수행하지 않음.
- 테스트: 테스트 파일 전무(`test_*.py` 0개), CI 설정 없음(`.github` 없음)(확인). 실행할 테스트 스위트 부재.
- 반환 계약 동적 확인(확인): `OllamaClient.is_available()` → `tuple` 반환, `GitManager.get_diff_stats()` → 길이 3 튜플, `GitManager.commit()` → 3-튜플 반환을 직접 관찰.

## 발견된 문제점 (확인 vs 추정, 심각도)

### [확인][심각도 높음] api_server.py의 코어 반환 계약 다수 불일치 → 런타임 크래시/오동작
`core` 모듈의 다수 메서드는 튜플을 반환하나 `api_server.py`가 스칼라로 취급했다. `main.py`는 동일 메서드를 올바르게 언패킹(`ok, msg = ollama.is_available()`, `ok, msg, commit_hash = git.commit(...)`)하는 반면 `api_server.py`만 어긋났다(확인).
- `/git/commit`: `message = ollama.generate_commit_message(...)`가 `(ok, msg)` 튜플을 `message`에 대입 → 이후 `gm.commit(message)` 내부 `message.strip()`에서 `AttributeError: 'tuple' object has no attribute 'strip'` 발생. 별도 스니펫으로 크래시 재현(확인).
- `/git/commit`: `commit_hash = gm.commit(message)`로 3-튜플을 단일 변수에 대입(확인).
- `/health`, `/ollama/status`: `ollama.is_available()` 튜플을 그대로 사용. `/ollama/status`의 `if available else []`는 비어있지 않은 튜플 `(False, "...")`이 항상 참이라 Ollama가 죽어도 모델 조회를 시도(확인).
- `/analyze/commit-message`, `/analyze/gitignore`, `/analyze/sensitive-files`: 각각 튜플 반환을 비언패킹 상태로 응답에 노출, 또는 `file_list`로 `List[str]`을 `str` 파라미터에 전달(확인, 후자는 크래시는 아니나 계약 위반).

### [확인][심각도 중간] requirements.txt 부정확
- `gitpython>=3.1.40`을 명시하나 코드 어디에서도 임포트하지 않음(subprocess 사용)(확인).
- `api_server.py`가 요구하는 `fastapi`/`uvicorn`/`pydantic`이 누락(확인). 결과적으로 명세대로 설치해도 API 서버 기동 불가.

### [확인][심각도 중간] 코어 모듈 중복본(루트 vs core/)으로 인한 드리프트
루트의 `git_manager.py`/`config_manager.py`는 `core/`판과 내용이 다름(확인). 예: 루트 `git_manager.push()`에는 PAT 주입 로직이 없고 core판에는 있음. 루트 `config_manager`는 기본 모델 `llama3.1:8b`, core판은 `llama3.2:3b`. 실제 사용되는 것은 `core/`이며 루트본은 미사용(확인). 유지보수 시 잘못된 파일을 수정할 위험.

### [확인][심각도 낮음] 기본 모델명 불일치
`core/config_manager.py` 기본 `ollama_model = "llama3.2:3b"`, `core/ollama_client.py`의 `OllamaConfig.model = "llama3.1:8b"`(확인). 설정을 명시 주입하지 않으면 클라이언트는 3.1:8b를 가정.

### [확인][심각도 낮음] .gitignore와 추적 파일 충돌
`.gitignore`가 `*.png`, `screenshots/*`를 무시하나 `screenshots/01_dashboard.png`는 이미 추적 중(확인). 기능 영향 없으나 의도와 불일치.

### [추정][심각도 중간] HTTPS PAT 임시 주입 방식의 누출 가능성
`core/git_manager.push()`는 PAT를 remote URL(`https://{pat}@...`)에 임시 삽입 후 복원한다(확인). 그러나 push 중 프로세스가 중단되면 토큰이 박힌 URL이 `.git/config`에 잔존할 수 있다(추정, 미재현). 또한 PAT가 평문 설정 JSON(`github_pat`)에 저장됨(확인).

### [추정][심각도 낮음] api_server CORS 전체 허용
`allow_origins=["*"]`로 모든 출처 허용(확인). 로컬 전용 도구라면 수용 가능하나 0.0.0.0 바인딩과 결합 시 노출 위험(추정).

## 조치한 내용

저위험·검증 가능한 수정만 적용했고, 적용 후 재컴파일 및 로직 시뮬레이션으로 검증했다.

1. `api_server.py` 반환 계약 정합화(확인 후 적용):
   - `/health`, `/ollama/status`: `available, detail = ollama.is_available()`로 언패킹, `detail` 필드 추가.
   - `/git/commit`: `get_diff_stats()` 3-튜플 언패킹 + 요약 문자열 생성, `generate_commit_message` `(ok, msg)` 언패킹(실패 시 502), `commit()` `(ok, out, hash)` 언패킹(실패 시 400). 기존 `tuple.strip()` 크래시 제거. git push 등 변경 동작은 추가하지 않음.
   - `/analyze/commit-message`, `/analyze/gitignore`, `/analyze/sensitive-files`: 각 튜플을 명시적 JSON 필드(`success`/`patterns`/`explanation`/`sensitive`/`reason` 등)로 변환, `file_list`를 개행 결합 문자열로 전달.
   - 검증(확인): `py_compile` 통과, 코어 모듈로 commit-msg 경로 언패킹 시뮬레이션 정상.

2. `requirements.txt` 정정(확인 후 적용): 미사용 `gitpython` 제거, `fastapi`/`uvicorn`/`pydantic` 추가, `requests` 유지, `PyQt6` 유지.
   - 검증(확인): 파일 내용 갱신 완료. 단, 환경 제약상 실제 `pip install`은 수행하지 않음.

전체 재컴파일(확인): `main.py`, `api_server.py`, `core/*.py` `py_compile` 재통과.

## 미해결·위험 항목

- API 서버 실기동 검증 미수행: fastapi/uvicorn/pydantic 미설치로 엔드포인트 통합 테스트 불가. 수정은 정적·시뮬레이션 검증까지만 완료(미해결).
- 코어 모듈 중복본 정리: 루트의 미사용 `*.py` 4종 삭제 또는 단일화 권고. 삭제는 동작 영향 가능성을 배제할 수 없어 본 감사에서는 미수행(권고만).
- 기본 모델명 불일치: `OllamaConfig.model`을 `llama3.2:3b`로 통일 권고. 단순하나 동작 가정 변경이라 미적용(권고).
- PAT 처리: push 중단 시 토큰 잔존 가능성 및 평문 저장. credential helper 사용 또는 환경변수 주입으로 전환 권고(위험, 설계 변경 필요, 미수행).
- 테스트/CI 부재: 최소 단위 테스트(git_manager 상태 파싱, version_manager 증가 로직)와 CI 추가 권고.
- GUI(`main.py`) 실행 검증: push 위험으로 미실행. 안전한 더미 저장소에서 수동 검증 필요.

## 종합 판단

AutoGit은 PyQt6 GUI와 FastAPI 서버가 공통 `core/` 모듈을 공유하는 로컬 Git 자동화 도구이다. 코어 로직과 `main.py`의 계약 정합성은 양호하나, `api_server.py`는 코어의 튜플 반환을 일관되게 잘못 다뤄 다수 엔드포인트가 오동작하거나(특히 `/git/commit`은 확정적 크래시) 잘못된 응답을 냈다(확인). 또한 `requirements.txt`가 API 서버 의존성을 누락해 명세대로는 서버를 띄울 수조차 없었다(확인).

본 감사에서 이 두 항목(반환 계약, requirements)을 저위험 수정으로 교정하고 정적·시뮬레이션 수준에서 검증했다. 다만 의존성 미설치로 API 서버의 실제 기동/통합 검증은 미완이며, 코어 중복본·모델명 불일치·PAT 처리·테스트 부재는 권고 수준으로 남겨 두었다.

현 상태(확인): GUI는 미검증이나 임포트/컴파일은 통과하여 실행 가능성은 높음(추정). API 서버는 의존성 설치 후 본 수정으로 정상 동작할 것으로 판단(추정, 실기동 미검증). 운영 투입 전 의존성 설치 + 안전 저장소 기반 통합 테스트 + 코어 중복본 정리를 권고한다.
