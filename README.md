# GitAutoPush
GitAutoPush is a desktop application that automates repetitive Git commit/push tasks. Use Local LLM (Ollama) to perform .gitignore analysis, commit message generation, and security checks.
